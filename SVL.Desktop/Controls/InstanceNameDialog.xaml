<Window x:Class="SVL.Desktop.Controls.InstanceNameDialog"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        mc:Ignorable="d"
        Title="创建新实例"
        Width="400"
        Height="Auto"
        SizeToContent="Height"
        WindowStyle="None"
        AllowsTransparency="True"
        Background="Transparent"
        WindowStartupLocation="CenterOwner"
        ResizeMode="NoResize">

    <Window.Resources>
        <BooleanToVisibilityConverter x:Key="BoolToVisibilityConverter"/>
    </Window.Resources>

    <!-- 背景遮罩 -->
    <Grid Background="{DynamicResource ColorBrushMask}">
        <Border Width="360"
                Background="{DynamicResource ColorBrushBackground}"
                CornerRadius="8"
                BorderBrush="{DynamicResource ColorBrush3}"
                BorderThickness="1">
            <Border.Effect>
                <DropShadowEffect Color="Black"
                                  Opacity="0.3"
                                  BlurRadius="20"
                                  ShadowDepth="0"/>
            </Border.Effect>

            <Grid Margin="24">
                <Grid.RowDefinitions>
                    <RowDefinition Height="Auto"/>
                    <RowDefinition Height="Auto"/>
                    <RowDefinition Height="Auto"/>
                    <RowDefinition Height="Auto"/>
                    <RowDefinition Height="Auto"/>
                    <RowDefinition Height="Auto"/>
                </Grid.RowDefinitions>

                <!-- 标题 -->
                <TextBlock Grid.Row="0"
                           Text="创建新实例"
                           FontSize="18"
                           FontWeight="SemiBold"
                           Foreground="{DynamicResource ColorBrush1}"
                           Margin="0,0,0,16"/>

                <!-- 提示文本 -->
                <TextBlock Grid.Row="1"
                           Text="请输入实例名称（将作为文件夹名称）:"
                           FontSize="13"
                           Foreground="{DynamicResource ColorBrushGray3}"
                           Margin="0,0,0,8"/>

                <!-- 输入框 -->
                <TextBox Grid.Row="2"
                         x:Name="InstanceNameTextBox"
                         FontSize="13"
                         Padding="10,8"
                         Background="{DynamicResource ColorBrushTransparentBackground}"
                         Foreground="{DynamicResource ColorBrush1}"
                         BorderBrush="{DynamicResource ColorBrush3}"
                         BorderThickness="1"
                         Margin="0,0,0,12">
                    <TextBox.Resources>
                        <Style TargetType="Border">
                            <Setter Property="CornerRadius" Value="4"/>
                        </Style>
                    </TextBox.Resources>
                </TextBox>

                <!-- 错误提示 -->
                <TextBlock Grid.Row="3"
                           x:Name="ErrorTextBlock"
                           Text="文件夹名称不能为空"
                           FontSize="11"
                           Foreground="#FF4488"
                           Margin="0,0,0,12"
                           TextWrapping="Wrap"
                           MaxHeight="48"
                           Visibility="Collapsed"/>

                <!-- Debug 模式复选框 -->
                <CheckBox Grid.Row="4"
                          x:Name="DebugModeCheckBox"
                          Content="安装失败时保留文件（用于调试）"
                          FontSize="12"
                          Foreground="{DynamicResource ColorBrushGray3}"
                          Margin="0,0,0,16"
                          IsChecked="False"
                          VerticalAlignment="Center"
                          ToolTip="启用后，如果安装失败，版本目录中的文件将被保留，方便排查问题"/>

                <!-- 按钮 -->
                <StackPanel Grid.Row="5" Orientation="Horizontal" HorizontalAlignment="Right">
                    <Button Content="取消"
                            Width="80"
                            Height="32"
                            FontSize="12"
                            Margin="0,0,8,0"
                            Click="Cancel_Click"
                            Background="Transparent"
                            Foreground="{DynamicResource ColorBrushGray3}"
                            BorderThickness="1"
                            BorderBrush="{DynamicResource ColorBrush3}"
                            Cursor="Hand">
                        <Button.Resources>
                            <Style TargetType="Border">
                                <Setter Property="CornerRadius" Value="4"/>
                            </Style>
                        </Button.Resources>
                    </Button>

                    <Button Content="确定"
                            Width="80"
                            Height="32"
                            FontSize="12"
                            Click="Confirm_Click"
                            Background="{DynamicResource ColorBrush2}"
                            Foreground="White"
                            BorderThickness="0"
                            Cursor="Hand">
                        <Button.Resources>
                            <Style TargetType="Border">
                                <Setter Property="CornerRadius" Value="4"/>
                            </Style>
                        </Button.Resources>
                    </Button>
                </StackPanel>
            </Grid>
        </Border>
    </Grid>
</Window>
